# weixin-decryptor-for-new-weixin

针对**新版微信（4.1.11+）**数据库密钥方案变更的工具与文档。

> ⚠️ **先读这一节，避免误期待**
>
> 本仓库**没有**提供可用的完整密钥提取器。
> 提供的是：**回归诊断**、**FTS 兜底方案**（已实测可用）、**静态偏移推导工具**（半成品）。
> 详见下方[能力矩阵](#能力矩阵)。

## 背景

社区工具 `wechat-decrypt` 通过扫描微信进程内存中的
`x'<64hex_enc_key><32hex_salt>'` 模式提取数据库密钥。该方案在
**微信 4.1.11+ 已完全失效**，且原项目 `ylytdeng/wechat-decrypt`
已于 2026-07-13 被 DMCA 下架。

失效的**实际后果**比"不能用"更糟：解密流程不报错，但会**静默跳过**拿不到
密钥的新消息分片，导致导出的记录**丢掉最近的全部消息**而使用者毫无察觉。

## 能力矩阵

| 能力 | 状态 | 说明 |
|---|---|---|
| 内存扫描取密钥 | ❌ 失效 | 机制已变，不可修复 |
| FTS 兜底补消息 | ✅ **已实测** | 在 4.1.13.65 上验证，817/917 锚点命中 |
| 新旧分片密钥缓存刷新 | 📋 已定位 | 设计缺陷，需 upstream 改 `main.py` |
| codec 偏移静态推导 | ⚠️ 半成品 | 能推出候选偏移，**但未经运行时验证** |
| 完整密钥提取（Frida） | ❌ 未提供 | 需 Frida spawn + 处理 anti-hook，见下 |
| 密钥持久化位置 | ✅ **已定位** | `all_users/login/<wxid>/key_info.db`（明文 SQLite），见 [03](docs/03-key-storage.md) |
| 离线解密 `key_info_data` | ❌ 已排除多条路径 | 包装密钥不落盘，详见 [03](docs/03-key-storage.md) |
| 非注入捕获（硬件断点） | ⚠️ **机制已验证** | 能附加、能命中、能读寄存器；登录瞬间未打通，详见 [04](docs/04-non-invasive-attempt.md) |

## 快速开始

### FTS 兜底：把丢掉的消息找回来

如果你的导出结果缺了最近的消息（尤其是微信更新过之后）：

```bash
# 预览，不覆盖原文件
python tools/recover_fts.py --decrypted-dir <解密目录> --messages data/messages.json

# 确认无误后就地更新
python tools/recover_fts.py --decrypted-dir <解密目录> --messages data/messages.json --in-place
```

### 诊断当前的密钥状态

```bash
python tools/find_wechat_codec_offset.py [Weixin.dll 路径]
```

未指定路径时自动在常见安装目录下按版本号找最新的 `Weixin.dll`。

输出示例：

```
[*] 微信版本: 4.1.13.65
[*] MMV1 字符串 VA: ['0x189242d48']
[+] 引用点(disp32) RVA 0x3634F4C
[+] 所属函数       RVA 0x3634F10 .. 0x3634F92  (130 字节)
[+] hook 偏移      Weixin.dll + 0x3634F10
```

## 文档

- [01 — 回归报告](docs/01-regression.md)：内存扫描为何失效，完整证据链
- [02 — FTS 兜底方案](docs/02-fts-fallback.md)：原理、ID 空间陷阱、用法
- [03 — 密钥持久化位置与包装结构](docs/03-key-storage.md)：**找到了密钥存哪儿，以及为什么离线解不开**
- [04 — 非注入路径尝试](docs/04-non-invasive-attempt.md)：**用硬件断点替代 Frida，机制已验证、登录瞬间未打通**

## 新机制是什么

```
password ≠ enc_key

enc_key = PBKDF2-HMAC-SHA512(password, salt, 256000, 32)
salt    = 数据库文件头前 16 字节
```

内存中保存的是 **password**（KDF 的输入），不是可直接使用的 `enc_key`。
所以"在内存里搜 enc_key"这条老路从根上就不通了。

获取 password 需要 **Frida spawn 早期注入**（attach 会错过），
且该路径**存在 anti-hook 检测**。

### 一个重要且容易被误解的点

失效的**不是密钥，是提取方法**。实测：18 个数据库中 17 个的 `salt` 自
2026-06 以来一字未变，其旧密钥至今仍能通过 HMAC 校验 —— 即
`password` **长期稳定**，微信并没有"轮换密钥"。

新出现的分片（`message_1.db` 等）之所以打不开，只是因为它是**新文件**、
有**新 salt**、因而有**新派生密钥** —— 这是正常建库行为。

> 所以不存在「每次微信更新都要重新对抗」的军备竞赛。
> 只要能拿到那个长期稳定的 `password` + 任意文件的 `salt`，
> 就能派生出该文件（含所有新分片）的 `enc_key`。

## 关于「静态偏移推导」这个半成品

社区方案把 hook 目标函数的偏移**硬编码**（例如 4.1.12.26 的 `0x3486140`），
微信每次更新都会失效——这正是本次踩坑的直接原因。

`tools/find_wechat_codec_offset.py` 改为**静态推导**：

1. 在 `Weixin.dll` 里定位 `MMV1` 魔数字符串，算出 VA
2. 扫描 `.text` 中 RIP-relative 引用该字符串的指令
3. 用 `.pdata`（x64 异常表）反查该指令所属函数的起始 RVA

换版本重跑即可，不需要人工逆向。

**但请注意**：推出的是**候选**偏移，尚未经运行时验证。在 4.1.13.65 上，
候选函数只有 130 字节、内容是加载魔数字符串后比较，**更像是日志/断言助手**，
不像接收 password 的配置入口。欢迎验证或修正。

## 密钥到底存在哪儿

已在 4.1.13.65 上定位：

```
<xwechat_files>/all_users/login/<wxid>/key_info.db     ← 明文 SQLite
  └─ LoginKeyInfoTable.key_info_data                   ← 180 字节 BLOB
       └─ 标准 AEAD 包装（BoringSSL：AES-GCM / ChaCha20-Poly1305）
            └─ 包装密钥：登录时获得，不落盘
```

**为什么找到位置也解不开**：AES-GCM 是公开标准，任何人都能实现 ——
**算法的公开性正是其安全性的来源**。缺的不是「实现算法的能力」，
是那 32 字节的密钥。已排除的路径（全部带 oracle 校验）：

- 180 字节内每个 32 字节窗口当 password → PBKDF2 校验 → 0
- blob 内的全局 32 字节常量当密钥 → AES-GCM/CBC/CTR、ChaCha20 交叉穷举 → 0
- 该常量是设备标识（MachineGuid/SID/主机名/用户名）的哈希派生 → 0
- DPAPI 包装 → 0
- MMKV / config 文件中的明文密钥 → 0

完整过程与复现方法：[docs/03-key-storage.md](docs/03-key-storage.md)

## 两条运行时路径的现状

| 路径 | 状态 | 说明 |
|---|---|---|
| **Frida 注入** | 社区已验证可行 | 4.1.12.26 上 25/25 库解密成功。代价：注入 + anti-hook 风险 |
| **硬件断点（非注入）** | **机制已验证，未打通** | 见 [04](docs/04-non-invasive-attempt.md) |

### 硬件断点路径已验证的部分

- ✅ 用 `DebugActiveProcess` 附加**不注入、不改代码**
- ✅ 用 `SizeOfImage` 在 138 个模块中唯一识别 `Weixin.dll`（避开 ASLR）
- ✅ 字节校验通过（目标内存 == 磁盘字节），证明断点位置正确
- ✅ 真实微信上捕获到 21 个 `rcx`，全链路（捕获 → 验证）跑通

### 卡住的三个具体障碍

1. **调试模式启动被反调试拦截** —— 进程存活、DLL 已加载，但始终不出窗口
2. **登出/登录会替换主进程** —— 单 PID 附加随登出失效
3. **全进程附加遇事件风暴** —— 300 秒产生 **1018 万个**调试事件（约 34,000/秒），
   0 命中。疑似微信自身做单步自陷（anti-debug）

## 未做的事 / 需要帮助的地方

1. **解决上述事件风暴** —— 是打通非注入路径的关键
2. **`key_info.db` 的包装算法**未逆向——相关函数已定位
   （见 [docs/03](docs/03-key-storage.md) 第六节）
3. 欢迎提交：在你自己环境上验证出的偏移、包装算法、或事件风暴的解法

## 免责声明

本仓库仅用于**本机、本人账号的聊天记录导出**（数据可携带性）。
不包含任何个人数据、账号标识、密钥值或聊天内容。
请遵守当地法律法规与软件服务条款。使用风险自负。

## License

MIT
